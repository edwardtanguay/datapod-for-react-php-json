import { Box } from 'lucide-react';
import ContentWrapper from '../components/ContentWrapper';

import React, { useEffect, useState } from "react";
import axios from "axios";

interface Article {
	suuid: string;
	description: string;
	price: number;
}

const API_URL = "http://localhost:3312/articles";

export const PageArticles = () => {

	const [articles, setArticles] = useState<Article[]>([]);
	const [formData, setFormData] = useState({ description: "", price: "" });
	const [editing, setEditing] = useState<Article | null>(null);
	const [loading, setLoading] = useState(false);

	const fetchArticles = async () => {
		setLoading(true);
		try {
			const res = await axios.get(API_URL);
			setArticles(res.data);
		} catch (err) {
			console.error("Error fetching articles:", err);
		} finally {
			setLoading(false);
		}
	};

	useEffect(() => {
		fetchArticles();
	}, []);

	const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
		setFormData({ ...formData, [e.target.name]: e.target.value });
	};

	const handleSubmit = async (e: React.FormEvent) => {
		e.preventDefault();
		try {
			const data = { ...formData, price: parseFloat(formData.price) };
			if (editing) {
				await axios.put(`${API_URL}/${editing.suuid}`, data);
			} else {
				await axios.post(API_URL, data);
			}
			setFormData({ description: "", price: "" });
			setEditing(null);
			fetchArticles();
		} catch (err) {
			console.error("Error saving article:", err);
		}
	};

	const handleEdit = (article: Article) => {
		setEditing(article);
		setFormData({
			description: article.description,
			price: article.price.toString(),
		});
	};

	const handleDelete = async (id: string) => {
		if (!window.confirm("Delete this article?")) return;
		try {
			await axios.delete(`${API_URL}/${id}`);
			fetchArticles();
		} catch (err) {
			console.error("Error deleting article:", err);
		}
	};



	return (
		<ContentWrapper icon={<Box size={32} className="text-slate-800" />} title="Articles" >
			<div className="min-h-screen bg-gray-50 p-6 flex flex-col items-center">
				<div className="w-full max-w-2xl bg-white rounded-xl shadow p-6">
					<h1 className="text-2xl font-semibold mb-4 text-center">
						Manage Articles
					</h1>

					<form
						onSubmit={handleSubmit}
						className="flex flex-col sm:flex-row gap-3 mb-6"
					>
						<input
							type="text"
							name="description"
							placeholder="Description"
							value={formData.description}
							onChange={handleChange}
							required
							className="flex-1 px-3 py-2 border rounded-md focus:ring focus:ring-blue-200"
						/>
						<input
							type="number"
							name="price"
							placeholder="Price"
							value={formData.price}
							onChange={handleChange}
							required
							className="w-32 px-3 py-2 border rounded-md focus:ring focus:ring-blue-200"
						/>
						<button
							type="submit"
							className="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md transition"
						>
							{editing ? "Update" : "Add"}
						</button>
					</form>

					{loading ? (
						<p className="text-center text-gray-500">Loading...</p>
					) : (
						<div className="overflow-x-auto">
							<table className="w-full border-collapse">
								<thead>
									<tr className="bg-gray-100 text-left">
										<th className="p-3 border-b">Description</th>
										<th className="p-3 border-b">Price (€)</th>
										<th className="p-3 border-b text-right">Actions</th>
									</tr>
								</thead>
								<tbody>
									{articles.map((a) => (
										<tr key={a.suuid} className="hover:bg-gray-50">
											<td className="p-3 border-b">{a.description}</td>
											<td className="p-3 border-b">{a.price.toFixed(2)}</td>
											<td className="p-3 border-b text-right space-x-2">
												<button
													onClick={() => handleEdit(a)}
													className="text-blue-600 hover:underline"
												>
													Edit
												</button>
												<button
													onClick={() => handleDelete(a.suuid)}
													className="text-red-600 hover:underline"
												>
													Delete
												</button>
											</td>
										</tr>
									))}
									{articles.length === 0 && (
										<tr>
											<td colSpan={3} className="text-center p-4 text-gray-500">
												No articles found.
											</td>
										</tr>
									)}
								</tbody>
							</table>
						</div>
					)}
				</div>
			</div>
		</ContentWrapper>
	);
}
